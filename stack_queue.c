#include "monty.h"

void stack(stack_t **stack, unsigned int line_number)
{
    /* Set the format of the data to a stack (LIFO) */
    (void)stack;
    (void)line_number;
}

void queue(stack_t **stack, unsigned int line_number)
{
    /* Set the format of the data to a queue (FIFO) */
    (void)stack;
    (void)line_number;
}
